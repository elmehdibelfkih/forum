// DONE: change the input methods (input to textarea)

// DONE: fix the maxlength and minlength of all inputs 
// the values: 
	EMAIL_MIN_LEN = 6
	EMAIL_MAX_LEN = 254

	// Username limitations
	USERNAME_MIN_LEN = 3
	USERNAME_MAX_LEN = 32

	// Password limitations
	PASSWORD_MIN_LEN = 8
	PASSWORD_MAX_LEN = 72

	// Post Title limitations
	TITLE_MIN_LEN = 1
	TITLE_MAX_LEN = 100

	// Post Content limitations
	POST_MIN_LEN = 1
	POST_MAX_LEN = 10_000

	// Comment limitations
	COMMENT_MIN_LEN = 1
	COMMENT_MAX_LEN = 1_000

// DONE: fix the pages titles

FIXME: 'there is an error should be handled, if credentials are invalid, you should redirect to \
//       the same page to clear the form, to avoid repeatitive'
