services:
  forum:
    container_name: forum
    image: forum:latest
    build: ./requirements
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - forum_db:/app/project/database
      # - forum_static:/app/static

volumes:
  forum_db:
  # forum_static:
